use metaverse_messages::capabilities::item::Item;

pub fn generate_model(outfit: Vec<Item>) {
    // override the texture, mesh and override the skeleton values.
    // if there is no mesh, use the default mesh.
    println!("aaa");
}
