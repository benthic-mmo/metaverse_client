use metaverse_messages::capabilities::item_data::ItemData;

pub fn generate_model(outfit: Vec<ItemData>) {
    // override the texture, mesh and override the skeleton values.
    // if there is no mesh, use the default mesh.
    println!("outfit: {:?}", outfit);
}
